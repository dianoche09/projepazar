# ProjePazar — ortam değişkenleri (kopyala → .env.local). Gizli; commit etme.

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=            # YALNIZ server/cron — client'a sızdırma

# --- App ---
NEXT_PUBLIC_APP_URL=https://app.projepazar.com
LEAD_SHARE_SECRET=                    # imzalı paylaşım linki token'ı (/p/...)
CRON_SECRET=                          # Vercel cron doğrulaması

# --- WhatsApp (Faz 2 / sadece butonlu teyit şablonu) ---
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_TOKEN=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_TEMPLATE_FRESHNESS=stok_teyit

# --- Claude API (Faz 2) ---
ANTHROPIC_API_KEY=
